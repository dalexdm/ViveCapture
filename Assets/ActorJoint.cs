using UnityEngine;
using System.Collections;

public class ActorJoint : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	public ActorJoint(Vector3 pos) {
		this.transform.position = pos;
	}
}
